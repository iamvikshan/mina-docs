# Amina API - Environment Variables
# Uses Doppler for environment management (doppler.com)
# Copy this to .env for local development without Doppler
# DO NOT commit .env to version control

# =============================================================================
# DOPPLER (automatic when using `doppler run`)
# =============================================================================
DOPPLER_ENVIRONMENT=dev

# =============================================================================
# REQUIRED - MongoDB Connection
# =============================================================================
# Get this from MongoDB Atlas:
# 1. Go to your cluster > Connect > Drivers
# 2. Select "Node.js" driver
# 3. Copy the connection string (includes database name in path)
# 4. Replace <password> with your password
MONGO_CONNECTION=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/amina?retryWrites=true&w=majority

# =============================================================================
# OPTIONAL - Discord Webhook for Logs
# =============================================================================
# 
# Discord Webhook URL for application logs
# Format: Shoutrrr URL scheme for Discord notifications
# Syntax: discord://[webhook-id]/[webhook-token]
# 
# To obtain your webhook URL:
# 1. Go to your Discord server settings
# 2. Navigate to Webhooks section
# 3. Create or select a webhook
# 4. Copy the webhook URL
# 5. Replace 'your-webhook-url-here' with the actual URL
#
# Note: Keep this URL confidential as it grants webhook access to your Discord channel
# This is optional - omit or leave commented if you don't need Discord log notifications

LOGS_WEBHOOK=discord://[webhook-id]/[webhook-token]
# =============================================================================
# OPTIONAL - Rate Limiting
# =============================================================================
# Salt used for hashing IP addresses in rate limiting
# If not provided, a random salt is generated on each worker start
# Keep this consistent across workers for predictable rate limiting

CLIENT_SECRET=

# =============================================================================
# OPTIONAL
# =============================================================================