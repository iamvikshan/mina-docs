{
  "name": "Ubuntu + Bun + Docker",
  "image": "vikshan/ubuntu-bun:latest",

  // Add features to the dev container, either from a Dockerfile or a list of features to install.
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {
      "installDirectlyFromGitHubRelease": true,
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    }
  },

  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "eamodio.gitlens",
        "oven.bun-vscode",
        "WakaTime.vscode-wakatime",
        "mongodb.mongodb-vscode",
        "coderabbit.coderabbit-vscode"
      ]
    }
  },

  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "[ -f scripts/iamvikshan.sh ] && chmod +x scripts/iamvikshan.sh; bun install",

  // Set `remoteUser` to `root` to connect as root.
  "remoteUser": "root"
}
